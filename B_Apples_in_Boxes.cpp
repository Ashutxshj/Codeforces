#include <iostream>
using namespace std;
typedef long long ll;
int t, k;
ll a, k;
char rem[2001000];
ll 
int main()
{
    cin >> t;
    while (t--)
    {
        cin >> n >> k >> (rem + 1);
        l[0] = r[n + 1] = 0;
        for (int i = 1; i <= n; i++)
        {
            cin>>dt[i];
        }
    }
}